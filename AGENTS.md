# Codex 作業規約

このリポジトリは、Minecraft Java 版（Paper）向けの「妨害＋PvE＋視聴者参加型」企画ツールである。

Codex は実装担当、ChatGPT は仕様策定・レビュー・受入判定担当とする。仕様の優先順位は次の通り。

1. ユーザーからその作業で明示された指示
2. `docs/00_FIXED_DECISIONS.md`
3. `docs/01_PRODUCT_SPEC.md`
4. `docs/02_ADMIN_UI_SPEC.md`
5. `docs/03_ARCHITECTURE.md`
6. `docs/04_DATA_AND_API.md`
7. `docs/05_DEVELOPMENT_PLAN.md`
8. その他の文書・既存実装

矛盾を発見した場合は独自判断で仕様を変更せず、作業報告の「確認事項」に記載する。ただし、コンパイル不能・データ破損・セキュリティ問題を防ぐための最小修正は行い、理由を報告する。

## Codex 使用量最適化

- 依頼されたPhaseと受入条件に必要な範囲だけを実装する。
- 全面リファクタリング、将来機能の先行実装、依存関係の総更新を行わない。
- 既存の共通Core・DTO・保存処理・テスト補助を再利用する。
- 同じ目的の処理を新設する前に、リポジトリ全体を横断検索する。
- 不明点があっても全作業を止めず、確定部分を進め、重大な分岐だけを確認事項に残す。
- ドキュメントの言い換えや大量生成を実装作業に含めない。

## 実装ルール

- ユーザーの日常運用でコマンド入力を要求しない。通常操作は管理画面またはゲーム内クリック操作で完結させる。
- 開発・自動テスト用の内部操作は許可するが、通常利用者向けコマンドとして公開しない。
- 管理画面、開発用アダプター、将来のYouTubeアダプターは同じApplication Serviceを呼び出す。
- Bukkit/Paper APIを非同期スレッドから直接操作しない。HTTP処理からのゲーム操作は必ずサーバーメインスレッドへ委譲する。
- ワールドのコピー、ZIP展開、ファイル削除など重いI/Oはメインスレッドで実行しない。
- ワールドZIPのZip Slip、パストラバーサル、シンボリックリンク、巨大展開を防ぐ。
- 管理APIから任意のサーバーコマンドや任意ファイル操作を実行できる機能を作らない。
- 設定保存は一時ファイルへの書込み後に置換し、直前の正常版をバックアップする。
- ゲーム生成物にはPersistent Data Container等で所有タグを付け、無関係なMob・ブロック・プレイヤーデータを削除しない。
- NMSは使用しない。Paper APIで実現できない場合のみ確認事項にする。
- ログへ認証トークン、Cookie、個人情報を出さない。
- UI表示、ユーザー向けエラー、READMEは日本語を基本とする。

## 変更範囲と回帰確認

- 作業開始時に、変更予定ファイルと触らない範囲を短く宣言する。
- 不具合修正では、可能な限り再現テストまたは回帰テストを追加する。
- セルフレビューは今回の変更に関連する同種不具合、横展開漏れ、未修正箇所に限定する。
- テストは「変更箇所の単体テスト → 関連モジュール → ビルド」の順に実行する。
- フルE2E、Fresh Clone、実Paperサーバー起動は、Phaseの受入条件にある場合または節目だけ実施する。

## 禁止事項

- 仕様にないYouTube接続・OAuth・コメント取得を先行実装しない。
- 初期版に外部DB、クラウドサービス、Node.js常駐プロセスを追加しない。
- マップ原本をゲーム中に直接変更しない。
- サーバールートや不明なパスを再帰削除しない。
- テストを通すためにテスト自体を弱めない。
- 無関係なユーザー変更を上書き・削除しない。
- 明示されていないPush、マージ、リリース公開を行わない。

## 作業報告の形式

作業完了時は次だけを簡潔に報告する。

1. 実装した内容
2. 変更した主なファイル
3. 実行したテストと結果
4. 未実装・既知の制限
5. 仕様確認が必要な事項
6. コミットIDまたはPR（作成を依頼された場合のみ）

## Code Review Rules

### ゲーム状態

- 状態遷移を迂回してゲームを開始・終了できないこと。
- 中止、例外、再起動の全経路で一時ワールドとプレイヤー状態が安全に回収されること。
- 過去セッションの非同期処理が新セッションへ影響しないよう、sessionIdを検証すること。

### ワールド保護

- 原本ワールドへゲーム中の変更を書き込まないこと。
- 削除対象は、プラグインが作成し所有確認できた一時ワールドだけであること。
- ZIP名やマップIDをファイルパスとして無検証で連結しないこと。

### 管理API

- ミューテーションは認証、Origin確認、入力検証、権限確認を通ること。
- API成功応答はPaper側処理の完了後に返し、単にキュー投入できたことを成功扱いしないこと。
- 任意コマンド実行・任意ファイル参照APIを作らないこと。

### ゲーム内所有物

- 敵、コア、表示、NPCはセッション所有タグを持つこと。
- クリーンアップは同じsessionIdの所有物だけを対象にすること。
- 一般ワールドの自然Mobや他プラグインのEntityを巻き込まないこと。
