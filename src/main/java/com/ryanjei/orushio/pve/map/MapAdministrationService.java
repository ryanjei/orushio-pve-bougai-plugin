package com.ryanjei.orushio.pve.map;import java.nio.file.Path;import java.util.*;public interface MapAdministrationService{List<MapProfile> list();Optional<String> fixedNextMapId();MapProfile importZip(Path zip,String mapId,String displayName);MapProfile setEnabled(String mapId,boolean enabled);void fixNext(String mapId);void delete(String mapId);SetupView startSetup(String mapId,UUID administrator,String expectedGameState);SetupView setup();void selectField(UUID administrator,String field,boolean area);void recordClick(UUID administrator,BlockPoint point,boolean secondaryCorner);MapProfile saveSetup(String expectedSessionId);void discardSetup(String expectedSessionId);record SetupView(boolean active,String mapId,String displayName,String administrator,String selectedField,boolean area,List<String> missing,Map<String,Integer> counts){public SetupView(boolean active,String mapId,String administrator,String selectedField,boolean area,List<String> missing){this(active,mapId,mapId,administrator,selectedField,area,missing,Map.of());}public static SetupView inactive(){return new SetupView(false,"","","","",false,List.of(),Map.of());}}}
